{{- if .Values.dockerSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: docker-hub-secret
  namespace: {{ .Values.namespaces.backend }}
type: kubernetes.io/dockercfg
data:
  .dockercfg: {{ .Values.dockerSecret | b64enc | quote }}

---
apiVersion: v1
kind: Secret
metadata:
  name: docker-hub-secret
  namespace: {{ .Values.namespaces.frontend }}
type: kubernetes.io/dockercfg
data:
  .dockercfg: {{ .Values.dockerSecret | b64enc | quote }}
{{- end }}
